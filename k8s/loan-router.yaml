---
apiVersion: v1
kind: Service
metadata:
  name: loan-router
  namespace: bizevents-lab
spec:
  selector:
    app: loan-router
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: loan-router
  namespace: bizevents-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loan-router
  template:
    metadata:
      labels:
        app: loan-router
    spec:
      containers:
      - name: loan-router
        image: igoroschsimoes/loan-router:v1.0.0
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        env:
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 3
